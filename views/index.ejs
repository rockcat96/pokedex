<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
<body>
    <%- include("./partials/header.ejs") %>
    <h4>Click on each Pokemon to get more information about the Pokemon!</h4>

    <a href="/pokemon/new"><button>Create New Pokemon</button></a>
    <article>
        <%for(poke of Pokemon){%>
            <h3><%= poke.name %></h3>
            
            <a href="/pokemon/<%= poke.id %>"><img src="<%= poke.img %>" 
            alt="<%= poke.name %>"></a><br>
            
            <a href="/pokemon/<%= poke.id %>/edit">
                <button>Edit</button>
            </a>

            <form action="/pokemon/<%= poke.id %>?_method=DELETE" method="POST">
                <input type="submit" value="Delete"/>
            </form>


        <%}%>
    </article>
    
</body>
</html>